class Heap {
  /* 
  Heap data structure is a complete binary tree that satisfies the heap property. It is also called as a binary heap.
  A complete binary tree is a special binary tree in which every level, except possibly the last, is filled.
  All the nodes are as far left as possible.
  */

  // Heapify is the process of creating a heap data structure from a binary tree. It is used to create a Min-Heap or a Max-Heap.
  heapify(array, size, i) {

  }

  // Peek operation returns the maximum element from Max Heap or minimum element from Min Heap without deleting the node.
  peek() {
    // return rootNode.
  }

  // Algorithm for insertion in Max Heap.
  insertMax(array, newNum) {

  }

  // Algorithm for deletion in Max Heap.
  deleteMax() {

  }

  // Algorithm for insertion in Min Heap.
  insertMin() {

  }

  // // Algorithm for deletion in Min Heap.
  deleteMin() {

  }
}

//(for max heap) key of each node is always greater than its child node/s and the key of the root node is the largest among all other nodes;
let maxHeap = new Heap();
array = [3, 9, 2, 1, 4, 5];

// maxHeap.insertMax(array, 3);
// maxHeap.insertMax(array, 4);
// maxHeap.insertMax(array, 9);